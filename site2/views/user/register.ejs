<!DOCTYPE html>
<html lang="en">
    <%- include('../partials/head')%>
<body>
    <!---- Comienzo del header -->  
    <%- include('../partials/header') %>   
      <main class="form-signin">
         
         <div id='register'>
            <h2 id="regTitle">Mis datos</h2>
            <% if (typeof errors != 'undefined') { %> 
               <ul>
                  <% for (error of errors) { %>
                     <li><%= error.msg  %> </li> 
                  <% } %> 
               </ul>
            <% } %>          
            <ul id="regErrors"></ul>
            <form class='form-log' action="/users/register" method="POST" enctype="multipart/form-data">
               <div id="reg-name">
                  <label for="name">Nombre</label>
                  <input type="text" name="name" id="regName">
                  <p class="errors" id="errorName">El campo debe tener al menos dos caracteres.</p> 
               </div>          
               <div id="reg-lastname">   
                  <label for="surname">Apellido</label> 
                  <input type="text" name="lastname" id="regLastname"> 
                  <p class="errors" id="errorlastname">El campo debe tener al menos dos caracteres.</p> 
               </div>
               <div id="reg-tel">
                  <label for="tel">Teléfono</label>
                  <input type="tel"  name="phone" id="regPhone"> 
               </div>
               <div id="reg-dni">
                  <label for="dni">DNI</label>
                  <input type="text" name="dni" id="regDni"> 
               </div>
               <div id="reg-gender">
                  <label for="gender">Género</label>
                  <select name="gender" id="regGender" >
                     <option value="fem">Femenino</option>
                     <option value="masc">Masculino</option>
                     <option value="other">Otro</option>
                  </select>
               </div>
               <div id="reg-email">
                  <label for="email">Correo electrónico</label>
                  <input type="email" name="email" id="regEmail">
                  <p class="errors" id="errorEmail">El contenido no es una direccion de correo válida.</p> 
               </div>
               <div id='reg-avatar'>
                  <label for="avatar">Imagen</label>
                  <input type="file" name="avatar" id="regAvatar">
               </div>
               <h3>Identificación</h3>
               <div id="reg-pass">
                  <label for="password">Contraseña</label>
                  <input type="password" name="password" id="regPassword" class="form-control" >
                  <p class="errors" id="errorpassword">La contraseña debe tener al menos ocho caracteres.</p> 
               </div>
               <div id="reg-retype">
                  <label for="retype">Repetir contraseña</label>
                  <input type="password" name="retype" class="form-control" >
               </div>
               <div>
                  <button class="w-100 btn btn-lg btn-primary" type="submit" value="Ingresar" id="submit">Crear mi cuenta</button>
               </div>
            </form>
         </div>
      </main>
    <%- include('../partials/footer')%>
</body>
</html>